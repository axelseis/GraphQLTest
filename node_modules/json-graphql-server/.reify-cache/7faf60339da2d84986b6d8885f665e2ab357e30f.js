"use strict";var makeExecutableSchema;module.watch(require('graphql-tools'),{makeExecutableSchema(v){makeExecutableSchema=v}},0);var printSchema;module.watch(require('graphql'),{printSchema(v){printSchema=v}},1);var getSchemaFromData;module.watch(require('./introspection/getSchemaFromData'),{default(v){getSchemaFromData=v}},2);var resolver;module.watch(require('./resolver'),{default(v){resolver=v}},3);




module.exportDefault(data =>
    makeExecutableSchema({
        typeDefs: printSchema(getSchemaFromData(data)),
        resolvers: resolver(data),
        logger: { log: e => console.log(e) }, // eslint-disable-line no-console
    }));
